package eu.ibagroup.formainframe.explorer

import com.intellij.openapi.actionSystem.AnAction
import com.intellij.openapi.actionSystem.AnActionEvent

class EditMember : AnAction() {
  override fun actionPerformed(e: AnActionEvent) {
//    if (e.place == "Working Set Tree") {
//      val currentFile = e.getData(ConnectorWindowKeys.CURRENTLY_SELECTED_FILE)
//      if (currentFile != null && !currentFile.isDirectory) {
//        try {
//          val content = VfsUtil.loadText(currentFile)
//          val document = EditorFactory.getInstance().createDocument(content)
//          val editor = EditorFactory.getInstance().createEditor(document)
//          FileEditorManager.getInstance(e.project!!).openFile(currentFile, true)
//        } catch (ioException: IOException) {
//          ioException.printStackTrace()
//        }
//      }
//    }
  }
}
